SECRET_KEY=wildguard-wayanad-2024-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

ADMIN_USERNAME=admin
ADMIN_PASSWORD=wayanad2024

CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ── MySQL ─────────────────────────────────────
# Change DB_PASSWORD to your MySQL root password
# If your MySQL has NO password, leave it empty
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=wildguard

# ── GPS simulation ────────────────────────────
# seconds between each GPS fix per elephant
# Use 10 for demo/viva, 60 for real use
GPS_INTERVAL_SECONDS=60

# ── Risk threshold for auto-alert ─────────────
RISK_THRESHOLD=0.70